/* exports.up = async function up(sql) {
  await sql`
    CREATE TABLE spotrating (
      id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
      username varchar(60),
			spots varchar
    )
  `;
};

exports.down = async function down(sql) {
  await sql`
    DROP TABLE spotfavorites
  `;
};
*/
