/*exports.up = async function up(sql) {
  await sql`
    CREATE TABLE spotrating (
      id varchar(60) PRIMARY KEY,
			user_rating varchar
    )
  `;
};

exports.down = async function down(sql) {
  await sql`
    DROP TABLE spotrating
  `;
};*/
